module.exports = {
  testEnvironment: 'node',
  testTimeout: 10000,
  setupFilesAfterEnv: [], // No global setup
  collectCoverage: false,
  verbose: true,
  forceExit: true,
  detectOpenHandles: true,
  maxWorkers: 1
};
